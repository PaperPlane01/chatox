spring:
  application:
    name: user-service
  data:
    mongodb:
      repositories:
        type: reactive
      database: chatox_user_service
      host: localhost
      port: 27017
server:
  port: 8083
eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://localhost:8081/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    appname: 'user-service'
    instance-id: 'user-service-${server.port}'
ribbon:
  eureka:
    enabled: true
jwt:
  public:
    key:
      location: jwt_public_key.txt
