import React, {FunctionComponent} from "react";
import {inject, observer} from "mobx-react";
import {Fab, createStyles, makeStyles} from "@material-ui/core";

interface CreateChatDialogFloatingActionButtonMobxProps {
    
}
